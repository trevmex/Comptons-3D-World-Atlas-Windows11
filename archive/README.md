# Local archive data

`Atlas-Online-Archive.html` is the local portal template. The actual `Mirror/` directory is generated on the user's machine by `scripts/Sync-AtlasLocalArchive.js` and is ignored by Git. It is not bundled because the historical pages/assets may have redistribution restrictions.
